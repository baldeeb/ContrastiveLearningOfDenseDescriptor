class TransformValidator():
    def __init__(self, transform):
        self.transform = transform

    def __call__(self, img):
        # TODO:
        #   - f = self.transform.forward(img)
        #   - visualize
        #   - i = self.transfrom.inverse(f)
        #   - visualize
        #   - i.norm().backward() -> check if img.grad is existent
        pass